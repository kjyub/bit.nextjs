"use client"

import * as S from "@/styles/BitMarketStyles"

interface IBitMarketTrade {
    marketCode: string
}
export default function BitMarketTrade({ marketCode }: IBitMarketTrade) {
    return (
        <S.TradeBox>

        </S.TradeBox>
    )
}